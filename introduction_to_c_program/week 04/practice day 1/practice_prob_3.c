#include <stdio.h>
#include <string.h>
int char_to_ascii(char n)
{
    int x = n;
    return x;
}
int main()
{
    char n;
    scanf("%c", &n);
    int x = char_to_ascii(n);
    printf("%d", x);
    return 0;
}